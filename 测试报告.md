# 部署测试报告

## 测试时间
2025-12-08 23:52

## 测试结果

### ✅ 后端服务 (端口8087)

**状态**: 运行正常
- 服务状态: `active (running)`
- 进程ID: 1578356
- 监听地址: `0.0.0.0:8087`
- 健康检查: ✅ 通过 (`{"status":"ok"}`)
- 根路径: ✅ 正常 (`{"message":"麻将馆记账系统API","version":"1.0.0"}`)
- API端点: ✅ 正常响应

### ✅ 前端服务 (端口88)

**状态**: 运行正常
- 服务状态: `active (running)`
- 进程ID: 1578419
- 监听地址: `localhost:88` 和 `172.200.200.254:88`
- 开发服务器: ✅ 正常启动
- 访问地址: http://localhost:88

### ✅ 端口监听

- 端口8087 (后端): ✅ 正常监听 `0.0.0.0:8087`
- 端口88 (前端): ✅ 正常监听 `:::88`

### ✅ 开机自启

- 后端服务: ✅ 已启用 (`enabled`)
- 前端服务: ✅ 已启用 (`enabled`)

## 访问地址

- **前端界面**: http://localhost:88
- **后端API文档**: http://localhost:8087/docs
- **后端健康检查**: http://localhost:8087/health
- **后端根路径**: http://localhost:8087/

## 测试命令

```bash
# 检查服务状态
sudo systemctl status mjg-backend.service
sudo systemctl status mjg-frontend.service

# 检查端口监听
netstat -tlnp | grep -E ':(8087|88)'

# 测试后端健康检查
curl http://localhost:8087/health

# 测试后端API
curl http://localhost:8087/api/customers

# 查看日志
sudo journalctl -u mjg-backend.service -f
sudo journalctl -u mjg-frontend.service -f
```

## 结论

✅ **所有服务部署成功，运行正常，已配置开机自启**

